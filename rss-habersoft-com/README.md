# main-service

`main-service`, rss.habersoft.com ekosisteminin merkezi sunucu uygulamasidir.

Uygulanan urun ve operasyon belgeleri icin bkz. [.docs/README.md](.docs/README.md).

Servisleri sade bir entegrasyon okuma sirasiyla yeniden kavramak icin bkz. [.docs/service-handbook/README.md](.docs/service-handbook/README.md).

Production operator guide: [PRODUCTION.md](PRODUCTION.md). Bu dosya insan sunucu operatoru icindir; current production status ve evidence sahibi [.docs/production-acceptance.md](.docs/production-acceptance.md) dosyasidir.

Bu surum container tabanli API/worker iskeleti, config dogrulama, PostgreSQL/Redis baglanti siniri, health yuzeyi, canonical PostgreSQL business schema migration temeli, API process'ine ozel tenant RS256 JWT/JWKS dogrulama altyapisi, tenant feed abonelik endpoint'leri, tenant basina Redis rate limiting davranisi, `GET /api/entries` hafif liste endpoint'i, `GET /api/entries/{id}/detail` detail endpoint'i, Agent `X-Agent-Key` authentication altyapisi, `POST /agent/heartbeat` current-state endpoint'i, read-only `GET /agent/feeds/due` endpoint'i, read-only `POST /agent/feeds/{feed_id}/new-guids` endpoint'ini, idempotent `POST /agent/entries` ingestion endpoint'ini, idempotent batch `POST /agent/feed-check-results` outcome ingestion endpoint'ini, worker-only BullMQ cleanup job runner'ini, protected read-only `GET /admin-api/operations/summary` aggregate admin route'unu, protected read-only `GET /admin-api/operations/drilldown` bounded admin drilldown route'unu, bounded `POST /admin-api/operations/feed-recheck-requests` admin action route'unu, authenticated admin feed onboarding `POST /admin-api/operations/feed-onboarding-requests` action route'unu ve production Compose/release package dogrulama araclarini icerir.

Application version: `0.1.0-ms-017`.
Application status: `MVP — Production Aktif`.
Master baseline: `rss-habersoft-master-v12`.

MS-022A repository-local source adds disabled-by-default admin auth/session routes for the not-deployed `rss-admin-ui` local full-stack rehearsal: `GET /admin-auth/session`, `POST /admin-auth/login`, and `POST /admin-auth/logout`. Backend admin auth defaults to `ADMIN_UI_AUTH_MODE=disabled`, has no default credential, and requires explicit `single_admin` configuration with a PBKDF2 password hash, session secret, Redis-backed server-side sessions, and an HttpOnly `SameSite=Lax` cookie. MS-022B adds secretless provisioning helpers and production activation validation docs for a future operator-authorized deployment, while the admin UI remains `MS-022B_PRODUCTION_ACTIVATION_PACKAGE_READY - NOT_DEPLOYED`. MS-025A updates the admin session cookie path to `Path=/` so the same opaque HttpOnly cookie authenticates `/admin-auth/*` and `/admin-api/*`; login/logout also clear the historical `Path=/admin-auth` cookie. MS-025A-R2 records operator-reported production acceptance for the read-only operations dashboard and admin-api proxy-template remediation. The backend package version remains `0.1.0-ms-017` because the admin operations route is protected by admin auth, read-only, local-release-candidate validated, and not a backend package release.

Admin auth provisioning helpers:

```bash
npm run admin-auth:hash
npm run admin-auth:secret
npm run admin-auth:verify-config
npm run production:admin-auth:diagnose:redacted -- --synthetic
npm run production:admin-auth:compose:verify
```

The helpers use the current PBKDF2 admin password hash contract, generate/validate session secrets, validate production-like admin auth config, and redact sensitive values by default. Real production secrets must remain outside Git and outside Codex control. See [.docs/admin-auth-production-activation.md](.docs/admin-auth-production-activation.md).

MS-024E records operator-reported production retest evidence after MS-024D: backend admin-auth config is live and valid in `main-service-api`, worker admin-auth env remains absent by design, and backend loopback `/admin-auth/session` returns `configured=true`, `authenticated=false`, `reason=unauthenticated` before login. If backend API/image/network/admin-auth env is recreated, also run `cd /opt/habersoft-rss/rss-admin-ui && npm run ops:compose:recreate` so the frontend Nginx proxy refreshes upstream/network references. `AUTH_CONFIGURED_UNAUTHENTICATED` is the expected pre-login state. MS-024F records operator-reported authenticated admin shell production acceptance for the current implemented status/auth shell scope; Codex did not independently perform a credentialed login, read real credentials, mutate production, or capture rollback baseline. `auth-smoke:redacted` remains a redacted regression/sanity tool, not a pending acceptance blocker unless new contradictory evidence appears.

MS-025A adds protected read-only `GET /admin-api/operations/summary`. It requires an authenticated admin session, returns `401` without metrics when unauthenticated, returns `501 not_configured` without metrics when admin auth is disabled, rejects write methods with `405`, and leaves unknown `/admin-api/*` paths as `404`. The response contains aggregate `dependencies`, `feeds`, `entries`, `ingestion`, `window`, `generatedAt`, and safe `notes` only. It never returns tenant identifiers, feed URLs, entry content, raw logs, raw request/response bodies, private hostnames, cookies, password hashes, session secrets, database/Redis URLs, Agent keys, Tenant tokens, JWT claims, or stack traces. MS-025A-R2_ADMIN_OPERATIONS_DASHBOARD_PRODUCTION_ACCEPTED_OPERATOR_REPORTED closes the read-only operations dashboard production acceptance by operator report only; Codex did not independently perform a credentialed production login, did not mutate production, and did not accept write/business features. See [.docs/admin-operations-summary-api.md](.docs/admin-operations-summary-api.md).

MS-025B-R1_OPERATIONS_DRILLDOWN_PRODUCTION_ACCEPTED_OPERATOR_REPORTED adds protected read-only `GET /admin-api/operations/drilldown`. It uses the same admin session, hard-coded `recentHours=24` and `maxRows=20`, opaque `displayId` hashes, public `sourceHost` only, and safe notes. Raw feed URL paths, query strings, entry content, raw logs, request/response bodies, private hostnames, tenant data, cookies, password hashes, session secrets, database/Redis URLs, Agent key material, Tenant bearer tokens, JWT claims, and stack traces remain excluded. MS-025A-R2 remains accepted; drilldown production acceptance is closed by operator-reported MS-025B-R1 live retest evidence. No production deployment was performed by Codex for MS-025B-R1. See [.docs/admin-operations-drilldown-api.md](.docs/admin-operations-drilldown-api.md).

MS-026A_BOUNDED_ADMIN_FEED_RECHECK_ACTION_LANDED_OPERATOR_DEPLOY_RETEST_REQUIRED adds bounded feed recheck action support through `POST /admin-api/operations/feed-recheck-requests`. The route requires an authenticated admin session, JSON input, `X-Admin-CSRF`, `X-Admin-Idempotency-Key`, an opaque drilldown `actionRef`, and a 300 second cooldown. It only requests the existing due-feed path by moving one eligible feed's `nextCheckAt` to now; it performs no synchronous external feed fetch, no entry mutation, no raw feed URL exposure, no Agent key or Tenant bearer token use, no Prisma migration, and no package version bump. No production deployment was performed by Codex for MS-026A; operator deploy/retest required remains. See [.docs/admin-operations-drilldown-api.md](.docs/admin-operations-drilldown-api.md).

MS-026B_OPERATOR_REPORTED_FEED_RECHECK_ROUTE_DEPLOYED_NO_ELIGIBLE_TARGET records operator-reported MS-026A production route smoke: backend/frontend rebuild and recreate passed, health and exact admin routes passed, feed-recheck `GET` returned JSON `405`, unauthenticated feed-recheck `POST` returned JSON `401`, browser login succeeded, and Operations Overview/Drilldown loaded. Production had no feeds, so feed recheck effect remains `NO_ELIGIBLE_FEED_RECHECK_TARGET` and `PENDING_NO_ELIGIBLE_FEED_RECHECK_TARGET` / `PENDING_NO_ELIGIBLE_TARGET`; no production feed creation or fake actionRef is authorized. Backend recreate automation is available as `npm run ops:production:recreate:api-worker -- --dry-run` and requires `-- --apply` for operator-owned mutation.

MS-026C keeps backend mutation operator-owned while composing it into the frontend one-command retest path. Operators may run `cd ../rss-admin-ui && npm run ops:production:retest -- --dry-run`, then `--retest-only` or `--apply` when authorized. Credential-free authenticated checks classify as `AUTHENTICATED_BROWSER_EVIDENCE_REQUIRED`; browser evidence verification uses `npm run ops:browser-evidence:verify` and may report `BROWSER_EVIDENCE_ACCEPTED_AUTHENTICATED_READ_ONLY`, `BROWSER_EVIDENCE_NO_ELIGIBLE_FEED_TARGET`, or future `BROWSER_EVIDENCE_FEED_RECHECK_EFFECT_ACCEPTED_OPERATOR_REPORTED`. Feed recheck effect remains `PENDING_NO_ELIGIBLE_FEED_RECHECK_TARGET` until a real eligible feed exists.

MS-026C-R1 records `SUCCESS_MS_026C_R1_OPERATOR_AUTOMATION_PRODUCTION_ACCEPTANCE_CLOSED_FEED_RECHECK_PENDING_NO_TARGET` from operator-reported production retest evidence only. The accepted automation/browser-evidence classes are `OPERATOR_PROMOTION_RETEST_REDACTED_OK`, `NGINX_ROUTE_PROOF_ACCEPTED`, `browser-evidence-verify-ok`, `BROWSER_EVIDENCE_ACCEPTED_AUTHENTICATED_READ_ONLY`, and `BROWSER_EVIDENCE_NO_ELIGIBLE_FEED_TARGET`; critical risk `none`; no production contact by Codex. Feed recheck effect remains `PENDING_NO_ELIGIBLE_FEED_RECHECK_TARGET`; No production feed was created, seeded, or faked; No fake actionRef was generated. Feed recheck effect acceptance remains future work requiring a real eligible production feed and redacted browser evidence.

MS-027A adds `SUCCESS_MS_027A_ADMIN_FEED_ONBOARDING_AND_ELIGIBLE_TARGET_READINESS_LANDED_OPERATOR_DEPLOY_RETEST_REQUIRED` for authenticated admin feed onboarding through `POST /admin-api/operations/feed-onboarding-requests`. The route requires an authenticated admin session, JSON input, `X-Admin-CSRF`, `X-Admin-Idempotency-Key`, public HTTPS feed URL validation, host cooldown, and idempotency. It stores a reserved admin onboarding relation, rejects reserved admin site client IDs in tenant JWTs, performs no synchronous external feed fetch, exposes no raw feed URL in response or evidence, and does not require a Prisma migration, dependency, or package version bump. Browser evidence includes `BROWSER_EVIDENCE_FEED_ONBOARDING_AVAILABLE`; operator route smoke classifies `FEED_ONBOARDING_ROUTE_SMOKE_ACCEPTED`. Codex did not perform production contact. No production feed was created, seeded, or faked. MS-027A-R2 later closes the production route-smoke retest residual by operator report only. Frontend validation is `npm run verify:admin-feed-onboarding`, `npm run test:admin-api-proxy-template`, `npm run test:fullstack`, and `npm run test:production-mode-rc`.

MS-027A-R1 adds `SUCCESS_MS_027A_R1_PRODUCTION_PROMOTION_IMAGE_FRESHNESS_REMEDIATION_LANDED_OPERATOR_RETEST_REQUIRED`. Backend `npm run ops:production:recreate:api-worker -- --apply` now builds a current-HEAD image, verifies `org.opencontainers.image.revision` and `org.opencontainers.image.source`, writes `deploy/runtime-image.env`, and then recreates API/worker. `--recreate-only` is restart-only and blocks as `backend_image_stale` unless the existing runtime image already matches HEAD. Validate the shared backend/frontend guard from `rss-admin-ui` with `npm run verify:production-image-freshness`; MS-027A-R2 later closes the image-freshness and feed-onboarding route-smoke production retest residual by operator report only.

MS-027A-R2 adds `SUCCESS_MS_027A_R2_PRODUCTION_PROMOTION_AND_FEED_ONBOARDING_ROUTE_SMOKE_ACCEPTANCE_CLOSED_OPERATOR_REPORTED` from operator-reported production evidence only. Backend route semantics are unchanged from MS-027A; the acceptance records `OPERATOR_PROMOTION_RETEST_REDACTED_OK`, `NGINX_ROUTE_PROOF_ACCEPTED`, `browser-evidence-verify-ok`, `BROWSER_EVIDENCE_ACCEPTED_AUTHENTICATED_READ_ONLY`, `BROWSER_EVIDENCE_FEED_ONBOARDING_AVAILABLE`, `MS-027A-R2_PRODUCTION_PROMOTION_IMAGE_FRESHNESS_ACCEPTED_OPERATOR_REPORTED`, and `MS-027A-R2_FEED_ONBOARDING_ROUTE_SMOKE_ACCEPTED_OPERATOR_REPORTED`; image freshness accepted; backend runtime image revision matched current HEAD; frontend runtime image revision matched current HEAD; feed onboarding route smoke accepted; authenticated browser evidence accepted. Feed recheck effect remains `PENDING_NO_ELIGIBLE_FEED_RECHECK_TARGET`; Feed recheck effect acceptance remains future work requiring a naturally existing eligible target and redacted browser evidence. No production feed was created, seeded, or faked. No fake actionRef was generated. There was no production contact by Codex. Validate the tracked closure from `rss-admin-ui` with `npm run verify:production-feed-onboarding-acceptance`; receipt path: `operator-state/admin-ui-production-activation/ms-027a-r2-promotion-feed-onboarding-route-smoke-accepted-operator-reported-receipt.json`.

MS-027B adds `SUCCESS_MS_027B_FEED_ONBOARDING_RECHECK_EFFECT_FLOW_LANDED_OPERATOR_DEPLOY_RETEST_REQUIRED` without changing backend route semantics. The existing onboarding route creates reserved backend feed/subscription state, the drilldown route can expose that state as an eligible row, and the feed recheck route reports the existing due-feed path without raw feed URL or actionRef exposure. Redacted evidence classifications now include `FEED_ONBOARDING_EFFECT_ACCEPTED`, `FEED_RECHECK_EFFECT_ACCEPTED`, `PENDING_FEED_ONBOARDING_ASYNC_PROCESSING`, `PENDING_FEED_RECHECK_COOLDOWN`, `FEED_ONBOARDING_REJECTED_SAFE_VALIDATION`, `FEED_RECHECK_ACTION_REJECTED_SAFE_VALIDATION`, and `OPERATOR_ACTION_REQUIRED_WITH_REDACTED_REASON`. Operator deploy/retest remains required. No production feed was created, seeded, or faked. No fake actionRef was generated. Validate from `rss-admin-ui` with `npm run verify:feed-onboarding-recheck-effect-flow`.

MS-027B-R1 adds `SUCCESS_MS_027B_R1_FEED_ONBOARDING_RECHECK_EFFECT_PRODUCTION_ACCEPTANCE_CLOSED_OPERATOR_REPORTED_EVIDENCE_AUTOMATION_LANDED` by operator report only. Backend route semantics are unchanged from MS-027B; the acceptance records `MS-027B-R1_FEED_ONBOARDING_RECHECK_EFFECT_PRODUCTION_ACCEPTED_OPERATOR_REPORTED`, `operator_reported`, `FEED_ONBOARDING_EFFECT_ACCEPTED`, `FEED_RECHECK_EFFECT_ACCEPTED`, `browser-evidence-verify-ok`, and `NGINX_ROUTE_PROOF_ACCEPTED`. `PENDING_NO_ELIGIBLE_FEED_RECHECK_TARGET is closed for the bounded MS-027B feed onboarding plus recheck effect scope`. Codex did not independently perform a credentialed production login; no production contact by Codex; no production mutation by Codex. Validate from `rss-admin-ui` with `npm run verify:production-feed-effect-acceptance`; receipt path: `operator-state/admin-ui-production-activation/ms-027b-r1-feed-onboarding-recheck-effect-accepted-operator-reported-receipt.json`.

MS-027B-R2 adds `SUCCESS_MS_027B_R2_EVIDENCE_AUTOMATION_REGRESSION_MODE_LANDED_OPERATOR_RETEST_OPTIONAL` in the admin UI evidence tooling only. Backend route semantics, schema, Prisma migrations, dependencies, and package version are unchanged. MS-027B-R1 feed onboarding plus recheck effect production acceptance remains accepted; already-present feed retests may classify `FEED_ONBOARDING_PREVIOUSLY_ACCEPTED_NOT_RETESTED`, `FEED_ONBOARDING_ALREADY_PRESENT_REGRESSION_NOT_APPLICABLE`, `FEED_ONBOARDING_ACCEPTANCE_LEDGER_CONTINUITY_OK`, and `RECHECK_EFFECT_ACCEPTED_REGRESSION_OK` when the prior ledger is tracked. Do not claim a fresh onboarding effect from an already-present feed regression retest. Validate from `rss-admin-ui` with `npm run verify:evidence-regression-mode`.

MS-018C operator-confirmed evidence'e gore `main-service` backend production'da aktiftir: internal/public live ve ready checks HTTP `200`, PostgreSQL/Redis/tenantAuth readiness `up`, API loopback upstream `127.0.0.1:3200`.

MS-019B collector-v2 operational evidence receipt partial accepted durumdadir: explicit production Compose context, runtime Git/image identity, service/port policy, migration, worker/scheduler, health, boundary, redirect, TLS ve point-in-time restart/OOM evidence passed. MS-019C production backup/restore receipt `PRODUCTION_BACKUP_RESTORE_VERIFIED` durumundadir. MS-019D-R1 checkout hygiene ve current release pointer evidence'i `PARTIAL_ACCEPTED` olarak kabul etti; forward rollback baseline state current pointer'dan external olarak kuruldu. MS-019E-R2 edge body-limit compatibility evidence'i `PASSED` durumundadir. MS-019F-R5 bounded 20-minute operational-smoke ve machine-safe error-signal evidence'i selected v3 sample timeline governance karariyla `SUCCESS_GOVERNANCE_ACCEPTED` durumundadir. Historical previous production pointer `NOT_RECORDED` ve non-blocking kalir; long-term stability evidence ise `NOT_APPLICABLE_BY_GOVERNANCE_DECISION` durumundadir. MS-020B ile `rss-admin-ui` read-only status dashboard contract'i repository-local olarak uygulanmistir, fakat admin UI production deployment ve `rss-panel.habersoft.com` aktivasyonu yapilmamistir. Contract ve receipt verifier semantics [.docs/production-operational-evidence.md](.docs/production-operational-evidence.md), [.docs/backup-and-restore.md](.docs/backup-and-restore.md), [.docs/production-checkout-and-release-pointers.md](.docs/production-checkout-and-release-pointers.md), [.docs/production-edge-body-limit.md](.docs/production-edge-body-limit.md) ve [.docs/production-operational-smoke-and-error-signals.md](.docs/production-operational-smoke-and-error-signals.md) dosyalarindadir.

MS-017C staging drill tarihsel kanittir: `STAGING_USES_PRODUCTION_IDP` decision altinda approved staging target'ta candidate deployment, synthetic sentinel, PostgreSQL `pg_dump -Fc` backup, off-host restore verification, rollback to `0.1.0-ms-016`, roll-forward to `0.1.0-ms-017`, final current pointer promotion ve final running services acceptance passed. Staging package source/image kimlikleri production identity degildir.

External registry publish, Git tag ve GitHub Release yapilmamistir. Bagimsiz Agent application ve bagimsiz Tenant applications bu backend status claim'inin kapsami disindadir. MS-025A adds one protected read-only admin route without Prisma schema or migration changes; MS-025B adds one protected read-only bounded drilldown route without Prisma schema, migration, dependency, or package-version changes. MS-026A adds one bounded admin feed recheck action without Prisma schema, migration, dependency, or package-version changes. MS-027A adds authenticated admin feed onboarding without Prisma schema, migration, dependency, or package-version changes. MS-027A-R2 changes no backend route semantics and records only operator-reported production promotion/feed-onboarding route-smoke acceptance. MS-027B changes no backend schema, dependency, package version, or route authority; it verifies the existing safe onboarding-to-drilldown-to-recheck effect flow. MS-027B-R1 changes no backend schema, dependency, package version, or route authority and records only operator-reported bounded feed onboarding plus recheck effect production acceptance. MS-027B-R2 changes no backend schema, dependency, package version, or route authority and records only admin evidence regression-mode semantics. MS-025A-R2 accepts only the operator-reported read-only admin operations summary dashboard scope, MS-025B-R1 accepts only the operator-reported read-only drilldown scope, and broader future business/admin write features are not accepted.
